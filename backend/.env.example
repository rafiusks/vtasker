# Server Configuration
PORT=8000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=vtasker

# Optional: Full Database URL (if you prefer to use this instead of individual settings)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/vtasker?sslmode=disable

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173 